import subprocess

subproess.run(["docker", "exec", "-it", "jupyter", "."])
#todo: 入出力は、バージョン、ベースのもととなるリポジトリ、を指定する。

#todo: docker run -dit --name my-running-script -v "$PWD"/work:/home/hoge/work docker_image_name
#todo: docker
